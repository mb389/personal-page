// import {  } from '../actions/travels';

export default function travels(state = {}, action) {
  switch (action.type) {
  // case :
  //   return {
  //   	sidebarOpen : action.value
  //   };
  default:
    return state;
  }
}
